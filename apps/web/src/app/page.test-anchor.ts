// This file intentionally references the root page during Story 1.4 review validation.
// It ensures the health dashboard remains reachable through the App Router entrypoint.
export { default as HomePage } from "./page";
